server:
  name: "gomokusvr"
  port: 50052
  host: "localhost"

nacos:
  server_addr: "mse-3cb9ef30-p.nacos-ans.mse.aliyuncs.com:8848"
  namespace_id: "public"
  group: "DEFAULT_GROUP"
  cluster_name: "DEFAULT"
  log_level: "info"

game:
  max_rooms: 1000           # 最大房间数
  max_players_per_room: 2   # 每个房间最大玩家数
  room_timeout_minutes: 30  # 房间超时时间（分钟）
  move_timeout_seconds: 60  # 下棋超时时间（秒）

room:
  cleanup_interval_minutes: 5  # 房间清理间隔（分钟）
  max_room_name_length: 50     # 房间名最大长度 